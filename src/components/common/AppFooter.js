import React from 'react';
import { Footer, FooterTab, Button, Icon } from 'native-base';

export default () => (
    <Footer>
      <FooterTab>
        <Button transparent>
          <Icon name='ios-call' />
        </Button>
      </FooterTab>
    </Footer>
  );
